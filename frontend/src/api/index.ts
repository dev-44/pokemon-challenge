/**
 * API Layer - Barrel Export
 *
 * This file serves as the main entry point for all API services.
 * It follows the Facade pattern to provide a clean interface.
 */

export { pokeAPI } from "./pokeapi";
export { backendAPI } from "./backend";
export { pokeApiClient, backendApiClient } from "./config";
